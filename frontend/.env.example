# Copy to .env or .env.local

# undeployed (local devnet), preview, or preprod
VITE_NETWORK_ID=undeployed

# Optional: connect to an already-deployed contract instead of deploying a
# fresh instance on every wallet connect.
# VITE_CONTRACT_ADDRESS=
